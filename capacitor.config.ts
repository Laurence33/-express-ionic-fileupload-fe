import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'file-upload-fe',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
